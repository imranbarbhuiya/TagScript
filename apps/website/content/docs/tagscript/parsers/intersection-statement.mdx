---
title: IntersectionStatementParser
---

import { Callout } from 'fumadocs-ui/components/callout';
import { Tabs } from 'fumadocs-ui/components/tabs';

The intersection parser checks that all of the passed expressions are true.
Multiple expressions can be passed to the parameter by splitting them with pipe (`|`).
The payload is a required message that must be split by pipe (`|`).
If the expression evaluates true, then the message before the pipe (`|`) is returned, else the message after is returned.

## Usage

<Tabs items={['ESM', 'CommonJs']}>
	```ts showLineNumbers tab="ESM" 
	import { Interpreter, IntersectionStatementParser } from 'tagscript';
	const ts = new Interpreter(new IntersectionStatementParser());
	```
	```js showLineNumbers tab="CommonJs"
	const { Interpreter, IntersectionStatementParser } = require('tagscript');
	const ts = new Interpreter(new IntersectionStatementParser());
	```
</Tabs>

## API

Check [IntersectionStatementParser](../../typedoc-api/tagscript/classes/IntersectionStatementParser) for the API documentation.

## For End Users

<Callout type="info" emoji={null}>
### Syntax
{/* prettier-ignore-start */}
```yaml
{all(expression|expression|...):message}
```
{/* prettier-ignore-end */}

### Examples

```yaml
{all({args}>=100|{args}<=1000):You picked {args}.|You must provide a number between 100 and 1000.}
# if {args} is 52
You must provide a number between 100 and 1000.
# if {args} is 282
You picked 282.
```

### Aliases

`intersection`, `all`, `and`

</Callout>
